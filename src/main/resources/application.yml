spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Chen781+-/
    url: jdbc:mysql://127.0.0.1:3306/api?useSSL=false

mybatis:
  mapper-locations: classpath:/mapper/*.xml
  type-aliases-package: com.api.pojo
appId:
appSecret:
format: